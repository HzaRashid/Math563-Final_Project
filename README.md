# Math563 – Final_Project

Make sure the test images are in the root directory of this project.
Might have to modify the file paths in the code accordingly.

We are not pushing the images to github because they are large and cause slow commits.

Some python code is already setup in the code/ directory. See test.py to get
started with blurring an image.